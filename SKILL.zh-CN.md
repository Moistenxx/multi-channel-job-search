# 多渠道求职 Skill（通用版）

这是一个**不绑定具体智能体平台**的求职搜索规范。首选使用 **Agent-Reach** 作为互联网能力层；它负责选择、安装、诊断和路由网页搜索、网页阅读、RSS、公开页面和用户授权浏览器等上游工具，但它不是证据本身。没有 Agent-Reach 时，可以使用当前智能体已有的联网搜索、浏览器、HTTP、RSS、PDF 或网页读取工具，并明确标记为降级模式。

## 如何开始

1. 如果系统有 `agent-reach`，先运行 `agent-reach doctor`，记录网页、搜索、RSS、GitHub、LinkedIn、浏览器等渠道的状态。
2. 如果没有安装，自动安装到用户目录：优先执行 `pipx install https://github.com/Panniantong/agent-reach/archive/main.zip`；`pipx` 不存在或执行失败时，用可用的 Python 3.10+ 创建 `~/.agent-reach-venv` 并安装到这个虚拟环境。不使用 `sudo`，不改系统目录，不把工具仓库写入当前工作目录。
3. 安装后运行 `agent-reach install --env=auto`（只检查）和 `agent-reach doctor`。用户级安装失败时记录原始错误并继续使用智能体已有联网能力，标记 `native-fallback`。需要系统级依赖或配置时才询问用户，不自动使用 `--system`。

## 强制先确认求职条件

在搜索公司或岗位之前，必须问清楚并让用户确认：

- 国家、省、市、区县、园区或允许的通勤范围；
- 行业、细分行业、岗位族，以及中英文关键词；
- `社招`、`校招`、`实习`、合同/派遣，或明确的组合；
- 公司范围：指定公司，还是该地区该行业内所有可发现公司；
- 时间范围，以及学历、经验、薪资、语言、工作许可、远程/现场等限制。

可直接使用：

> 请确认搜索地点（国家/省市/区县或通勤范围）、行业、岗位及中英文关键词、社招/校招/实习、公司范围、时间范围和学历/经验/薪资等限制。只搜索你指定的地区，不会自行扩大范围。

条件不完整时只能追问，不能自行选择城市、行业或招聘类型，也不能沿用上一次请求的地点。

## 先找公司，再找岗位

先建立公司目录，再逐家公司找岗位。使用 Agent-Reach 或当前智能体可用工具搜索：

- 人社局、人才服务中心、公共人才网、开发区、国资委、国企/央企、行业协会、商会和高校就业网；
- 公司中文名、英文名、简称、法律实体、集团、子公司、分公司、园区、年报、投资者关系页面；
- 公司官网、招聘页、公开 ATS、官方公众号和有日期的招聘公告。

### 逐家公司强制门禁

公司目录保存后，**每一家企业都必须完成三类检查后才能结束运行**：

1. 天津市及所属区县的人社局、人才服务中心、公共人才网、开发区/园区人才页；
2. 公司官网、招聘页、ATS、法定名称及母子公司招聘入口；
3. 公司认证公众号、招聘账号及有日期的招聘文章。

每类都必须写入 coverage：`attempted`、打开/无匹配/阻塞状态、页数、查询词、观察数、岗位数、停止原因和 blocker。未尝试写 `not_attempted`，表示搜索未完成，不表示没有岗位。任一公司缺少任一类记录，最终状态必须是 `partial-incomplete-company-passes`，并列出未完成公司，不能声称已经全部搜索。

公司分为两条轨道：

- **国内企业**：中资、国企、集体企业或本地注册企业，并记录依据；
- **国外企业**：外资、合资、跨国公司或境外总部企业，并记录本地法律实体、分支机构和中英文招聘域名。

不能根据品牌名、语言或 Logo 猜企业性质；证据不足写“未知”。没有匹配岗位的公司也要保留，写“未发现可验证岗位”或具体阻塞原因。

## 岗位必须具体到公司

**没有公司，就不能算正式岗位。**“某知名企业”“某公司”只能列为线索。每条正式岗位至少绑定：

- 公司标准名、法律实体、集团/子公司关系；
- 国内/国外分类及依据；
- 官网、招聘系统或 ATS；
- 官方公众号名称、主体和文章链接（如使用）；
- 具体岗位、地点、社招/校招/实习类型、发布时间/截止时间；
- 投递链接、岗位编号、原始确认页和找到它的公司查询。

同一公司同一岗位在多个来源出现时合并计数，但保留全部来源和冲突字段。

## 社招、校招、实习分开查

- 社招：企业官网/ATS、人社局、人才服务中心、官方账号、国企渠道；关键词 `社会招聘`、`社招`、`experienced hire`。
- 校招：高校就业中心、企业校园页、校园公众号、双选会和人才引进公告；关键词 `校园招聘`、`校招`、`应届`、`graduate`、`campus`。
- 实习：企业实习页、高校实习渠道、人才服务中心和有日期的公告；关键词 `实习`、`intern`、`internship`。

来源没有明确招聘类型时写“未知”，不能自行归类。

## 搜索引擎和访问模式

Agent-Reach 是能力层，不是万能爬虫。使用它路由的上游工具直接读取公开页面，并记录 `agent-reach doctor` 的状态；某渠道不可用时切换到当前智能体的公开搜索或可见浏览器，并记录 `native-fallback`。

每次运行声明一种模式：

- `public-only`：公开网页、官网、ATS、RSS、PDF、公开公众号；
- `public-discovery-only`：只查企业、政府、高校和公开公众号，按用户要求排除大平台；
- `browser-assisted`：用户在可见浏览器中自行登录后搜索；
- `hybrid`：公开渠道加用户授权的可见浏览器渠道。

搜索结果提到某个平台，不等于平台已经被打开。登录墙、验证码、风控、robots、动态渲染、PDF 无法读取、无匹配结果和限流都必须写进覆盖记录。

## 登录和安全

需要登录时，打开正常网页并请用户自行登录，只回复“已登录”即可。不得索要或保存密码、短信验证码、Cookie、Token、私聊内容或简历。验证码、风险控制或身份确认出现时停止并交还用户。搜索和阅读不等于授权投递、上传简历、联系 HR、关注账号或改账号设置；每次外部操作都要临时再次确认。

## 证据等级和状态

- **A 官方确认**：官网或官方 ATS 明确显示公司、岗位、地点、投递路径及开放/未过期状态；
- **B 平台确认**：招聘平台的单条详情页可访问，含实质字段和投递路径；
- **C 线索**：摘要、聚合页、转载、列表页或不完整公众号文章；
- **D 无法验证**：登录墙、验证码、风控、撤下或缺少可操作投递证据。

只有 A/B 且有明确开放信号的岗位才能描述为“当前招聘”；没有日期或截止时间写“未知”，不能凭搜索排名推断。

## 输出

每次搜索生成：

- `job-search-YYYYMMDD-HHmm-companies.csv`：公司目录；
- `job-search-YYYYMMDD-HHmm-records.csv`：去重后的岗位；
- `job-search-YYYYMMDD-HHmm-observations.csv`：去重前所有来源记录；
- `job-search-YYYYMMDD-HHmm-full.md`：完整明细和来源；
- `job-search-YYYYMMDD-HHmm-summary.md`：中文汇总、覆盖情况、缺口和阻塞。

汇总必须分别统计国内/国外企业、社招/校招/实习、A/B/C/D、当前/过期/未知、重复记录和各渠道覆盖。不能声称“全网全部”，只能说明本次声明的来源、查询、日期和可访问页面范围。
